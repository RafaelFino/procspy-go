{
    "db_path": "data",
    "log_path": "logs",  
    "interval": 5,
    "config_url": "https://rgt-tools.duckdns/config.json",
    "targets": [
        {
            "name": "calc",
            "limit": 20,
            "pattern": "*calc*",
            "kill": true

        }
    ]
}